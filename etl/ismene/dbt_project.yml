name: 'ismene'
version: '1.0.0'
config-version: 2

profile: 'ismene'

model-paths: ["models"]
macro-paths: ["macros"]

seeds:
  ismene:
    employment_mapping_reference:
      +schema: Staging
    geo_country_mapping:
      +schema: Staging
    national_accounts_indicator:
      +schema: Staging
    government_sectors:
      +schema: Staging
    national_accounts_indicator_expenses:
      +schema: Staging


models:
  ismene:
    Core:
      +schema: "Core"
    Staging:
      +schema: "Staging"
    Mart:
      +schema: "Mart"

